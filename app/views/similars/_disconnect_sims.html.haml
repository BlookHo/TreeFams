//:javascript
//  $(function(){
//    var
//$('.arrays').append('profilesRewrite = ' + profilesRewrite);
//if (current_question_index == 0){
//  $('#showPreviousQuestion').hide();
//}

//  })


-#%h2 Разъединение объединенных "Похожих" профилей
-#%h2 Массив @profiles_to_rewrite:      #{@profiles_to_rewrite}
-#%h2 Массив @profiles_to_destroy:     #{@profiles_to_destroy}
%br

.disconnect
  %h2 Разъединение объединенных "Похожих" профилей
  %br

  = link_to 'Разъединить', disconnect_similars_path( profiles_rewrite_arr: @profiles_to_rewrite, profile_destroy_arr: @profiles_to_destroy), method: :post, remote: true, class: [:bttn, :blue], id: 'disConnectSimilars'
.after_disconnect
  %h2