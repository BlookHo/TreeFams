$( "#circle-table" ).animate({
    left: "-=60",
    opacity: "0"
  }, 500, function() {
    $('#current-circle').html("<%= j render 'main/circle_table', circle: @circle %>");
    $('#current-circle-path').html("<%= j render 'main/circle_path' %>")
  });
