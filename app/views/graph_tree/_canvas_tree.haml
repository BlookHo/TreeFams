%div{ :id => 'error_output', :height => '100%' }

%div{ :id => 'tree_canvas', :style => 'border: 1px solid #000;width:1000px;height:1000px;' }

%script{ :type => 'text/javascript', :defer => 'defer' }
  :plain
    var error = document.getElementById('error_output');
    var constructTree = new reTree(error, eval('(#{@profiles_json})'));