<%- if @error %>
alert('<%= @error %>');
<%- else %>
var profileId = "<%= params[:profile_to_rename].to_i %>";
console.log(" In rename.js: profileId = " + profileId );

console.log("@profile.name_id = <%= @profile.name_id %>");

var oneProfile = "<%= @profile %>";
console.log(oneProfile);
console.log(" In rename.js: oneProfile.name_id = " + oneProfile.name_id );
var newNameId = "<%= @new_name_id %>";
//console.log(newNameId);
console.log(" In rename.js: newNameId = " + newNameId );
$('#profile-context-menu').hide();
window.location.href = '<%= home_path %>';


<%- end %>
