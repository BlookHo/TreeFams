<% if !@tree_array.blank? %>

    <div>

    <table>
      <tr>
        <th>ID</th>
        <th>Родство</th>
        <th>Имя</th>
        <th>Пол</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <% @tree_array.each do |tree| %>
          <tr>
            <td><%= tree[0] %></td>
            <td><%= tree[1] %></td>
            <td><%= tree[2] %></td>
            <td><%= tree[3] %></td>
            <td> Вызов меню Родственника: <%= link_to "Relative Menu", main_relative_menu_path %> </td>
          </tr>
      <% end %>
    </table>
    </div>


    <h2>Совпадения древа Пользователя с другими</h2>
    <div>
      <p>Найдено совпадений: @match_qty_loc: <%= @match_qty_loc.inspect %></p>
      <p>Из них - подтвержденных: @approved_match_qty_loc <%= @approved_match_qty_loc.inspect %></p>
    </div>
    <h2> Переход на страницу Просмотра и Подтверждения совпадений:  <%= link_to "Подтверждения совпадений -->", main_match_approval_path %> </h2>



<% else %>
    <h1>No Listing tree!</h1>
    <p>Переход на страницу Старт:  <%= link_to "Старт -->", start_enter_myself_path %> </p>
    <p>Возврат на страницу Логин:  <%= link_to "Логин -->", pages_login_path %> </p>

<% end %>